/* Public settings only. Never put ADMIN_TOKEN or FORM_SECRET in a website file.
   Set apiBase to your NEW Cloudflare Worker origin after following SETUP.md. */
window.PodaturpetCommunity = Object.freeze({ apiBase: '' });
