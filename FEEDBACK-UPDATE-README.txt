WEBSITE FEEDBACK UPDATE
Copy website/ contents into your existing website folder. Replace matching files.
Keep your .git folder. No files need deleting.

git add .
git commit -m "Add bilingual website feedback popup with WhatsApp"
git push origin main

Feedback tab on every active HTML page. Redirect pages continue to their destination.
Automatic invitation after 25 seconds, once per tab session; avoids active typing.
English/Tamil form with suggestion, information, correction and question choices.
Up to 1500 characters; whitespace-only input rejected. Public page path included.
No message storage, automatic sending, paid service or API required.
WhatsApp opens in the same tab. Visitor must press Send there.
Close and Escape supported; manual dialog uses native modal focus handling.
Privacy page updated. Existing contact and buyer controls retained.
